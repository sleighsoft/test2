name: Inputs action
on:
  workflow_call:
  workflow_dispatch:

jobs:
  callme:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/withinputs
        with:
          option: option10
